%% Sequential Extreme Learning Algorithm - HAR : Random weights 
%--------------------------------------------------------------------------
%  
%  Random weights initialization.
%  
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%% ======================== Assign Weights ================================

function w = sela_random_weights(w1, w2)

w = zeros(w2, 1 + w1);
delta = 0.12;
w = rand(w2, 1 + w1) * 2 * delta - delta;

end

% =========================================================================
%% End